commands:
  01-install-node:
    command: "yum install nodejs npm --enablerepo=epel -y || :"
  02-public-build:
    command: mkdir -p public/build

container_commands:
  03-install-node-modules:
    command: "sudo -u webapp npm install --ddd --logs-dir=. || :"

container_commands:
  04-other:
    command: mkdir -p brian

container_commands:
  05-npm-build:
    command: "sudo -u webapp npm run build --ddd --logs-dir=. || :"
